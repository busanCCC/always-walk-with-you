# Task ID: 6
# Title: 영성일기 캘린더 구현
# Status: pending
# Dependencies: 1, 4
# Priority: medium
# Description: 월간 캘린더 뷰와 일기 작성 여부를 시각화하는 기능을 구현합니다.
# Details:
1. 캘린더 라이브러리 통합 (react-native-calendars 등)
2. 월간 캘린더 컴포넌트 구현:
```typescript
function JournalCalendar({ onDayPress }: Props) {
  const [selectedDate, setSelectedDate] = useState(new Date());
  const { data: journalDates } = useQuery(['journalDates'], fetchJournalDates);
  
  // 작성된 일기가 있는 날짜에 마커 표시
  const markedDates = useMemo(() => {
    // journalDates를 기반으로 마커 데이터 생성
  }, [journalDates]);
  
  return (
    <Calendar
      markedDates={markedDates}
      onDayPress={(day) => {
        setSelectedDate(new Date(day.timestamp));
        onDayPress(day);
      }}
    />
  );
}
```
3. 일기 작성 여부에 따른 날짜 마커 표시
4. 날짜 선택 시 해당 일의 일기 표시 또는 작성 화면으로 이동

# Test Strategy:
1. 캘린더 렌더링 및 날짜 선택 테스트
2. 일기 작성 날짜 마커 표시 테스트
3. 날짜 이동 및 월 변경 테스트
4. 데이터 로딩 상태 테스트
