# Task ID: 9
# Title: 순 그룹 관리 기능 구현
# Status: pending
# Dependencies: 1, 3
# Priority: high
# Description: 순 그룹 생성, 조회, 초대 등의 기능을 구현합니다.
# Details:
1. 순 그룹 목록 화면 구현:
```typescript
function GroupListScreen() {
  const { data: groups, isLoading } = useQuery(
    ['groups'],
    fetchUserGroups
  );
  
  return (
    <View>
      <FlatList
        data={groups}
        renderItem={({ item }) => <GroupCard group={item} />}
        ListHeaderComponent={<CreateGroupButton />}
      />
    </View>
  );
}
```
2. 순 그룹 생성 기능:
   - 그룹명, 캠퍼스 입력
   - 초기 멤버 추가
3. 그룹 초대 기능:
   - 이메일 또는 학번으로 검색
   - 초대 링크 생성 (선택적)
4. 그룹 상세 화면:
   - 멤버 목록
   - 공유된 일기 및 기도제목 목록
   - 그룹 설정 (관리자용)

# Test Strategy:
1. 그룹 목록 로딩 및 표시 테스트
2. 그룹 생성 및 초대 기능 테스트
3. 권한에 따른 기능 접근 테스트
4. 그룹 데이터 업데이트 테스트
