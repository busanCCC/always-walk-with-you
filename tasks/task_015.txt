# Task ID: 15
# Title: 마이페이지 및 업적 시스템 구현
# Status: pending
# Dependencies: 3, 7
# Priority: medium
# Description: 사용자 프로필 관련 기능을 포함한 마이페이지를 구현합니다.
# Details:
1. 마이페이지 화면 UI 구현:
```typescript
function MyPageScreen() {
  const { user } = useAuthStore();
  
  return (
    <ScrollView>
      <ProfileCard user={user} />
      <StatsSection userId={user.id} />
      <SettingsSection />
    </ScrollView>
  );
}
```
2. 프로필 카드 컴포넌트 구현:
   - 사용자 정보 표시
   - 프로필 이미지 변경 기능
3. 설정 섹션 구현:
   - 계정 설정
   - 알림 설정
   - 앱 정보

# Test Strategy:
1. 프로필 정보가 올바르게 표시되는지 테스트
2. 프로필 이미지 변경 기능 테스트
3. 설정 섹션의 각 기능이 정상 작동하는지 테스트
